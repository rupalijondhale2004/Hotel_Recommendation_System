<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Review</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css"/>

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>

  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .box {
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
      width: 40%;
      padding: 40px;
      margin: 60px auto;
      background-color: #fff;
    }

    .form-control {
      height: 45px;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .alert {
      margin-bottom: 25px;
      text-align: center;
      max-width: 100%;
      background-color: #fff3cd;
      color: #856404;
      border: 1px solid #ffeeba;
      border-radius: 5px;
      padding: 12px 20px;
   }


    .btn-row {
      display: flex;
      justify-content: center;
      gap: 15px;
    }

    .btn-orange {
      background-color: #ff8800;
      color: #fff;
      font-weight: 600;
      width: 150px;
      height: 45px;
      border: none;
      border-radius: 5px;
      text-align: center;
      line-height: 45px;
      transition: background 0.3s;
      text-decoration: none;
    }

    .btn-orange:hover {
      background-color: #e67300;
      color: #fff;
    }

    .btn-orange i {
      margin-right: 6px;
    }

    @media (max-width: 768px) {
      .box {
        width: 90%;
      }

      .btn-row {
        flex-direction: column;
        align-items: center;
        gap: 10px;
      }

      .btn-orange {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <div class="box">
    <h4 class="text-center text-uppercase mb-4" style="color:#ff8800;">Add Review</h4>

    <form action="/savereview" method="post">

      <div class="form-group">
        <input type="text" class="form-control" id="rev_text" name="rev_text" placeholder="Enter Review" required />
      </div>

      <div class="form-group">
        <input type="text" class="form-control" id="rating" name="rating" placeholder="Enter Rating" required />
      </div>

      <div class="form-group">
        <input type="text" class="form-control" id="rev_date" name="rev_date" placeholder="Enter Review Date" required />
      </div>

      <!-- Message Box -->
      <% if (msg) { %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
          <strong><%= msg %></strong>
        </div>
      <% } %>

      <!-- Buttons -->
      <div class="btn-row">
        <input type="submit" class="btn-orange" value="Add Review"/>
        <a href="/" class="btn-orange"><i class="fas fa-arrow-left"></i>Back</a>
      </div>

    </form>
  </div>

  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.3/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/js/bootstrap.min.js"></script>

</body>
</html>
