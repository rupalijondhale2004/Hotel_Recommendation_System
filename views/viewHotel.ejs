<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Hotel Rooms</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      background: linear-gradient(to right, #dfe9f3, #ffffff);
      padding: 20px;
    }

    .container {
      display: flex;
      gap: 24px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .card {
      background: #ffffff;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
      width: 350px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border: 1px solid #e0e0e0;
      position: relative;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 12px 28px rgba(0, 0, 0, 0.18);
    }

    .card img {
      width: 100%;
      height: 220px;
      object-fit: cover;
    }

    .card-content {
      padding: 20px;
    }

    .card-content h3 {
      margin: 0 0 10px;
      font-size: 26px;
      color: #0a6d5a;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    .card-content .address {
      display: flex;
      align-items: center;
      font-size: 14px;
      color: #666;
      margin-bottom: 8px;
      font-weight: 500;
    }

    .card-content .address::before {
      content: "📍";
      margin-right: 6px;
    }

    .card-content span {
      display: inline-block;
      font-size: 13px;
      background: #e8f5e9;
      color: #0a6d5a;
      padding: 4px 10px;
      border-radius: 20px;
      margin-bottom: 6px;
      margin-right: 8px;
    }

    .card-content p {
      margin: 6px 0;
      font-size: 14px;
      color: #555;
    }

    .rating {
      margin-top: 12px;
      font-weight: bold;
      font-size: 16px;
      color: #f39c12;
      display: flex;
      align-items: center;
    }

    .rating::after {
      content: "★";
      margin-left: 6px;
      color: #f39c12;
      font-size: 16px;
    }

    .book-btn {
      margin-top: 14px;
      width: 100%;
      padding: 12px;
      background: #0a6d5a;
      color: #fff;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s;
    }

    .book-btn:hover {
      background: #074e3e;
    }

    .book-btn a {
      color: #fff;
      text-decoration: none;
      display: block;
    }
  </style>
</head>
<body>

  <div class="container">
    <% data.forEach((item, index) => { %>
      <div class="card">
        <img src="/image/<%= item.filename %>" alt="Hotel Image"/>

        <div class="card-content">
          <h3><%= item.hotel_name %></h3>

          <p class="address">Address: <%= item.hotel_address %></p>

          <span><%= item.city_name %></span>
          <span><%= item.area_name %></span>

          <p class="rating">Rating: <%= item.rating %></p>

          <button type="submit" class="book-btn">
            <a href="/viewspecifichotel" target="_blank">View Hotel</a>
          </button>
        </div>
      </div>
    <% }); %>
  </div>

</body>
</html>
