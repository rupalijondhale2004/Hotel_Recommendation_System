<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Hotel Rooms</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      background: linear-gradient(to right, #dfe9f3, #ffffff);
      padding: 20px;
      overflow:auto;
    }

    .container {
      display: flex;
      gap: 24px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .card {
      background: #ffffff;
      border-radius: 16px;
      
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
      width: 350px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border: 1px solid #e0e0e0;
      position: relative;
      border: 2px solid #000;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 12px 28px rgba(0, 0, 0, 0.18);
    }

    .card img {
      width: 100%;
      height: 220px;
      object-fit: cover;
    }

    .index-badge {
      position: absolute;
      top: 12px;
      left: 12px;
      background: #0a6d5a;
      color: #fff;
      padding: 6px 12px;
      border-radius: 10px;
      font-size: 13px;
      font-weight: 600;
    }

    .card-content {
      padding: 20px;
    }

    .card-content h3 {
      margin: 0 0 10px;
      font-size: 24px;
      color: #0a6d5a;
    }

    .card-content .address {
      font-weight: bold;
      color: #444;
      margin-bottom: 6px;
      font-size: 14px;
    }

    .card-content span {
      display: inline-block;
      font-size: 14px;
      color: #666;
      margin-bottom: 4px;
      margin-right: 10px;
    }

    .card-content p {
      margin: 6px 0;
      font-size: 14px;
      color: #555;
    }

    .rating {
      margin-top: 10px;
      font-weight: bold;
      color: #e67e22;
      font-size: 15px;
    }

    .book-btn {
      margin-top: 14px;
      width: 100%;
      padding: 12px;
      background: #0a6d5a;
      color: #fff;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s;
    }

    .book-btn:hover {
      background: #074e3e;
    }

    .book-btn a {
      color: #fff;
      text-decoration: none;
      display: block;
    }
  </style>
</head>
<body>

  <div class="container">
    <% data.forEach((item, index) => { %>
      <div class="card">
        <!-- <p class="index-badge"><%= index + 1 %></p> -->

        <img src="/image/<%= item.filename %>" alt="Hotel Image"/>

        <div class="card-content">
          <h3><%= item.hotel_name %></h3>
          <p class="address"><span>Address: <%= item.hotel_address %></span></p>
          <span><%= item.city_name %></span> | <span><%= item.area_name %></span>

          <p class="rating">Rating: <%= item.rating %> ⭐</p>

          <button type="submit" class="book-btn">
            <a href="/viewspecifichotel?id=<%=item.hotel_id%>" target="_blank">View Hotel</a>
          </button>
        </div>
      </div>
    <% }); %>
  </div>

</body>
</html>
